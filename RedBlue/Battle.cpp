#include "RedBlue.hpp"

namespace RedBlue {

/*
Battle::Battle()
: hot(Player::a) 
, cold(Player::b)
, active(Table())
, cores(Table())
{
}

Active& Battle::hot_active() {
  return active[hot];
}

Active& Battle::cold_active() {
  return active[cold];
}

Core& Battle::hot_core() {
  return cores[hot][hot_active().core_idx];
}

Core& Battle::cold_core() {
  return cores[cold][cold_active().core_idx];
}
*/

}
